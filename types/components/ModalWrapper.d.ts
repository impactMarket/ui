import { GeneratedPropTypes } from '../types';
import React from 'react';
declare type ModalProps = {
    isActive?: boolean;
    children?: any;
    onCloseButton?: Function;
} & GeneratedPropTypes;
export declare const ModalWrapper: React.FC<ModalProps>;
export {};
