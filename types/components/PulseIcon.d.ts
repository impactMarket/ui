import { GeneratedPropTypes } from '../types';
import { colors } from '../theme';
import React from 'react';
declare type PulseIconProps = {
    icon: string;
    size?: number;
    borderColor?: keyof typeof colors;
} & GeneratedPropTypes;
export declare const PulseIcon: React.FC<PulseIconProps>;
export {};
