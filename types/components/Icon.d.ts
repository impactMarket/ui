import { GeneratedPropTypes } from '../types';
import { SizeType } from '../utils/setSize';
import React from 'react';
declare type IconProps = {
    icon: string;
    size?: SizeType;
    onClick?: () => void;
} & GeneratedPropTypes;
export declare const Icon: React.FC<IconProps>;
export {};
