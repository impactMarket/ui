import { GeneratedPropTypes } from '../types';
import React from 'react';
export declare type ThumbnailProps = {
    disabled?: boolean;
    handleClick?: any;
    icon?: string;
    url?: string;
} & GeneratedPropTypes;
export declare const Thumbnail: (props: ThumbnailProps) => React.JSX.Element;
